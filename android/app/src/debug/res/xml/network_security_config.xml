<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILDS ONLY. This file lives in src/debug/, so it is never merged into a
  release APK. A release build keeps Android's default, which refuses cleartext
  outright.

  It exists so the app can talk to a `next dev` server over plain HTTP while
  testing against a local lacrypta-crm. 10.0.2.2 is the Android emulator's alias
  for the host loopback; localhost covers a device on adb reverse.

  Nothing else is exempted: every other host still requires TLS even in debug.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
