<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--
      Debug-only overlay. The manifest merger applies this on top of src/main
      for debug builds and ignores it entirely for release, so the cleartext
      exemption in @xml/network_security_config cannot reach production.

      tools:replace is needed because src/main declares no networkSecurityConfig;
      without it the merger leaves the attribute unset.
    -->
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:networkSecurityConfig" />
</manifest>
